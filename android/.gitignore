*.iml
.gradle/
local.properties
.idea/
build/
captures/
.externalNativeBuild/
.cxx/
google-services.json
.kotlin/

# Release signing — never commit the upload key or its passwords
keystore.properties
*.jks
*.keystore

# Build artefacts do not belong in the repo; the AAB is rebuilt with
# `./gradlew bundleRelease` and lands in app/build/outputs/bundle/release/.
*.aab
*.apk
